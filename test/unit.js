// Unit tests are now handled by our custom TypeScript test files
// The actual tests are in src/**/*.test.ts and run via npm run test:ts

// This file exists to satisfy the ioBroker testing framework expectations
// The real unit tests are run via the test:ts npm script
console.log('Unit tests are handled by TypeScript test files in src/**/*.test.ts');
console.log('Run with: npm run test:ts');